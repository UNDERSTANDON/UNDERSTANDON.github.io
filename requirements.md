Website: generic multi-pages Computer sales system.

Pages:
From Assignment, need at least:
     ◦ A Home page 
     ◦ A Contact Page
     ◦ A Login page (check validate form using JavaScript)
     ◦ A Register page (check validate form using JavaScript)
     ◦ A Profile page
     ◦ A My Cart Page

However, just follow the \Reference\. to build the prototype.

Website type: Static
Require to host on GitHub Pages.
Storage or DB? Use either localstorage or JSON file. There is no mention for DB in the assignment.

For languages used, assignment requires: JS, CSS, HTML. But we can use any frameworks we want such as TailswindCSS, React and more. 
However, for the submission, we need to make sure that the code is clean and well-organized. So make sure that the code is readable and maintainable.
Animated elements can be implemented later if the exoskeletons are done.

For js, groups functions into different files. (e.g., auth.js, cart.js, etc.)
For style, group styles into different files. (e.g., auth.css, cart.css, etc.)
For html, group pages into different files. (e.g., auth.html, cart.html, etc.)

For folder, create a folder for each page. (e.g., auth/, cart/, etc.)

The main entrance is index.html and then we nagivate through the website using the header and footer.

Code should be in \src\.